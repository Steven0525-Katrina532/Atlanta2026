<!doctype html>
<html>
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title>Login · ATL Driver Scheduler</title>
  <style>
    body{font-family:ui-sans-serif,system-ui,Segoe UI,Roboto,Arial;margin:0;background:#f8fafc}
    .wrap{max-width:520px;margin:10vh auto;padding:24px;border:1px solid #e2e8f0;border-radius:16px;background:#fff}
    h1{margin:0 0 8px}
    .row{display:flex;gap:8px;align-items:center}
    input,button{width:100%;padding:10px;border:1px solid #cbd5e1;border-radius:8px}
    button{cursor:pointer}
    .error{color:#b91c1c;margin-top:8px}
    .muted{color:#64748b;margin-top:8px}
  </style>
  <link rel="stylesheet" href="/styles/tokens.css"></head>
<body>
  <div class="wrap">
    <h1>ATL Driver Scheduler</h1>
    <p class="muted">Sign in to continue.</p>
    <div class="row"><input id="email" placeholder="email" autocomplete="username"></div>
    <div class="row" style="margin-top:8px"><input id="pw" type="password" placeholder="password" autocomplete="current-password"></div>
    <div class="row" style="margin-top:12px"><button id="loginBtn">Sign In</button></div>
    <div id="err" class="error"></div>
  </div>

  <script type="module">
    import { initializeApp } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-app.js";
    import { getAuth, onAuthStateChanged, signInWithEmailAndPassword } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-auth.js";

    const cfg = {
      apiKey: "AIzaSyA9zGySiV_LYI-Vs0acQn1jhKdst_jzClW",
      authDomain: "atl-driver-scheduler-demo.firebaseapp.com",
      projectId: "atl-driver-scheduler-demo",
      storageBucket: "atl-driver-scheduler-demo.appspot.com",
      messagingSenderId: "586233326021",
      appId: "1:586233326021:web:4e11034eeaaab1ff5ec3ac"
    };
    const app = initializeApp(cfg);
    const auth = getAuth(app);

    document.getElementById("loginBtn").addEventListener("click", async ()=>{
      const err = document.getElementById("err");
      err.textContent = "";
      const email = document.getElementById("email").value.trim();
      const pw    = document.getElementById("pw").value;
      try { await signInWithEmailAndPassword(auth, email, pw); }
      catch(e){ err.textContent = (e?.code||"") + " " + (e?.message||"Login failed"); }
    });

    onAuthStateChanged(auth, (u)=>{
      if (u) { window.location.href = "/driver.html"; }
    });
  </script>
</body>
</html>




