{
  "name": "atl-scheduler-fns",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@google-cloud/tasks": "^6.2.1",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.1",
    "luxon": "^3.7.2",
    "pdfkit": "^0.13.0"
  },
  "scripts": {
    "build": "tsc -p .",
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions"
  },
  "devDependencies": {
    "@types/luxon": "^3.7.1",
    "typescript": "^5.9.3"
  }
}
